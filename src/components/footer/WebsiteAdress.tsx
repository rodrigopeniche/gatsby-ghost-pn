import * as React from "react"

const WebsiteAdress = () => {
    const adressStyles: React.CSSProperties = {
        fontFamily: "Georgia",
        fontStyle: "normal",
        fontWeight: "normal",
        fontSize: "16px",
        lineHeight: "18px",
        color: "#000000",
        margin: "82px auto 30px auto",
    }

    return <h1 style={adressStyles}>WWW.PHONENUMBERS.ORG</h1>
}

export default WebsiteAdress
